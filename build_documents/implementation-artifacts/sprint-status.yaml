# generated: 2026-05-05
# last_updated: 2026-05-29
# 2026-05-29: Story 6-4 -> review (dev-story / Amelia). Epic 6 wrap:
#             per-turn `turn.complete` rollup log (stt_ms/ttft_ms/
#             ttfb_ms/end_to_first_real_audio_ms/opener_*/
#             emphasis_count_per_turn/routing/had_tool_call); replaced
#             the end_to_transcript_ms=0 placeholder (renamed stt_ms);
#             tools/soak_v2.py aggregator + justfile recipe;
#             embodiment-brief amendment review (open-str-not-Literal
#             wording corrected). Full 7-day soak RUN deferred to 5-4.
#             All Epic-6 stories (6-1..6-4) now at review.
# 2026-05-29: Story 6-3 -> review (dev-story / Amelia). Emphasis as
#             the 7th vocalization tag: Talker `*word*` marks ->
#             splitter char-by-char parse (EmphasisStart/End sentinels)
#             -> Segment.emphasis_word_indices -> runtime join x
#             Story-6.1 SegmentTiming -> vocalization(tag="emphasis",
#             audio_frame_id="seg-N-w-<ms>"). expression_map.yaml
#             vocab 6->7; tag stays open str (NOT a Literal — YAML is
#             the gate); schema_version=3 unchanged. Closes DR-002 +
#             DR-004 wire side.
# 2026-05-29: Story 6-2 -> review (dev-story / Amelia). Cached
#             function-bucketed opener system (5 buckets) + LLM
#             <opener bucket=".."/> tag + timer fallback; retired
#             the Story 5.5 filler design; deleted the ~1 s
#             serialization tax (await filler_task) for Cartesia
#             overlap; manifest schema 1 -> 2 (opener surface).
#             Design-checkpoint: shipped cached as specified
#             (default presumption; cold-path p50 467 ms).
# 2026-05-28: Story 6-1 -> review (dev-story / Amelia). Cartesia
#             SSE->WS migration + timestamps capture + 500-sample
#             TTFB spike. Headline: cold p50 = 467 ms (above DR-001
#             0.4 s threshold), warm p50 = 230 ms (below). Spike
#             report writes the DR-001 keystone closure.
# 2026-05-05: Epic 2 stories prepared (Amelia / bmad-agent-dev / bmad-create-story);
#             epic-2 -> in-progress, all 5 stories -> ready-for-dev.
# 2026-05-06: bmad-correct-course ran against the spec-triple direction shift
#             (PRD/brief/distillate at commit 6f3bfe3 + architecture at ed8b276).
#             Restructured Epics 3 + 4 + 5; barge-in moved to v1.5 backlog.
#             Net v1 story count: 22 -> 30 across 5 epics. Epic 1 + Epic 2
#             (12 stories at 'review') untouched.
# 2026-05-07: Stories 3.1-3.7 prepared (Amelia / bmad-agent-dev / bmad-create-story);
#             epic-3 -> in-progress, all 7 stories -> ready-for-dev.
# 2026-05-12: Story 5-5 added (cached audio for deterministic phrases:
#             greetings, goodbyes, clarifications, thinking fillers).
#             v1 story count: 30 -> 31. Status -> ready-for-dev.
# 2026-05-28: v2 expression promotion (DR-001/002/003/004 design pass, commit ccfc0a0).
#             Initial draft added Epic 6 (Conversational Openers, 5 stories)
#             + Epic 7 (Speech Timing & Head Motion, 4 stories) -> backlog;
#             consolidated 2026-05-28 on user pushback into a single cohesive
#             Epic 6 "v2 Expression Upgrades" with 4 stories:
#               6.1 Cartesia WS + timestamps + TTFB spike (shared enabler)
#               6.2 cached opener system + Cartesia overlap (supersedes 5-5)
#               6.3 emphasis as the 7th vocalization tag (per DR-004)
#               6.4 instrumentation + soak + olaf-embodiment-brief amendment
#             Added Story v2-1 (events.jsonl sink, DR-003 promotion path)
#             under new v2-backlog status. v1 scope unchanged. Story 5-5
#             stays at 'review' — remains v1's shipping perceived-latency
#             surface until Epic 6 lands and 6.2 supersedes its filler
#             design (cached-audio infrastructure is REUSED). v1 story
#             count unchanged at 31; v2 adds 4 stories (Epic 6) + 1 v2
#             backlog (v2-1).
# project: olaf_companion
# project_key: NOKEY
# tracking_system: file-system
# story_location: build_documents/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Project-specific extension statuses (not standard bmad):
#   - v1.5-backlog: Deferred from v1; tracked for v1.5 planning. Not in current v1 scope.
#   - v2-backlog:   Deferred to v2 standalone backlog (not Epic 6/7 which are full v2 epics).
#                   Tracked for v2 planning; activated only on demand (DR-003 promotion path).
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-05
last_updated: 2026-06-04  # Story 6-5 -> ready-for-dev (create-story / Amelia)
project: olaf_companion
project_key: NOKEY
tracking_system: file-system
story_location: build_documents/implementation-artifacts

development_status:
  # ───────────────── Epic 1: Listen — wake-word + on-device STT ─────────────────
  epic-1: in-progress
  1-1-project-bootstrap-toolchain: review
  1-2-config-loaders-and-schema-validation: review
  1-3-logging-structlog-redaction-rotation: review
  1-4-event-schemas-error-hierarchy-protocol-seams: review
  1-5-audio-capture-mic-input-device-pinning: review
  1-6-wake-word-detection-porcupine: review
  1-7-vad-bounded-capture-and-stt: review
  epic-1-retrospective: optional

  # ───────────────── Epic 2: Speak — Talker + Cartesia ─────────────────
  epic-2: in-progress
  2-1-audio-playback-speaker-pinning: review
  2-2-talker-client-anthropic: review
  2-3-cartesia-client-tts: review
  2-4-turn-router-and-clarification: review
  2-5-pipeline-assembly-simple-turn: review
  epic-2-retrospective: optional

  # ───────────────── Epic 3: Embodiment Channel — four typed event topics + mood ─────────────────
  # Restructured 2026-05-06 (5 -> 7 stories):
  #   3.1, 3.2, 3.3 carried over (minor revisions). 3.4 NEW (event schema rebuild).
  #   3.5 REBUILT (was old 3.4 — four-topic Ros2EventPublisher + LogEventPublisher).
  #   3.6 NEW (mood module). 3.7 EVOLVED (was old 3.5 — audio-frame metadata + Talker SSML prompt).
  epic-3: in-progress
  3-1-expression-map-loader: review
  3-2-mapping-resolver-and-cache: review
  3-3-streaming-ssml-state-machine: review
  3-4-event-schema-rebuild: review
  3-5-event-publisher-ros2-and-log-adapter: review
  3-6-mood-module-state-and-controller: review
  3-7-audio-frame-metadata-and-ssml-prompt: review
  epic-3-retrospective: optional

  # ───────────────── Epic 4: Activity FSM + Tool-Use + Slow Path ─────────────────
  # Renamed 2026-05-06 (was "Complex Questions & Lifecycle"). Restructured 5 -> 7 stories:
  #   4.1, 4.2 carried over (minor AC tweaks). 4.3 NEW (activity FSM core, replaces old 4.4
  #   lifecycle with 7-state + deferred-sleep). 4.4 NEW (Talker tool-using upgrade).
  #   4.5 NEW (wake greeting). 4.6 NEW (mic-mode flip). 4.7 EVOLVED (merges old 4.3 + 4.5
  #   for slow-path wiring + complex-turn integration).
  epic-4: in-progress
  4-1-belief-state-client: review
  4-2-orchestrator-client-sse: review
  4-3-activity-fsm-core: review
  4-4-talker-tool-using-upgrade: review
  4-5-wake-greeting: review
  4-6-mic-mode-flip: review
  4-7-turn-router-slow-path-and-complex-turn: review
  epic-4-retrospective: optional

  # ───────────────── Epic 5: Production Hardening ─────────────────
  # Restructured 2026-05-06 (5 -> 4 stories): old 5.1 (barge-in) moved to v1.5 backlog.
  # Internal renumber: old 5.2 -> 5.1, 5.3 -> 5.2, 5.4 -> 5.3, 5.5 -> 5.4 (with expanded
  # sign-off list covering NFR30/31/32 + intent-sleep FP/FN + mood cadence).
  epic-5: backlog
  5-1-sighup-atomic-swap: backlog
  5-2-security-and-config-hardening: backlog
  5-3-systemd-service-deployment: backlog
  # 5-5 was added 2026-05-12 (cached audio for deterministic phrases:
  # greetings, goodbyes, clarifications, thinking fillers). Must land
  # BEFORE 5-4 — the soak measures NFR1 latency + Cartesia cost baselines,
  # which 5-5 changes substantially.
  #
  # 2026-05-28 RE-SEQUENCING (no label change): Epic 6 (v2 expression
  # upgrades) also changes NFR1 / NFR4 / NFR5 substantially — DR-001
  # projects ~3.0s → ~1.7-2.0s median end-of-speech → real-answer, plus
  # introduces emphasis as the 7th vocalization tag. If Story 5-4's soak
  # runs before Epic 6, it measures a baseline about to be replaced.
  # By the same logic that put 5-5 before 5-4, Epic 6 also goes before 5-4.
  # PRD / architecture / brief continue to label Epic 6 as "v2 work" — this
  # is execution sequencing only, not a re-scope.
  #
  # 2026-05-28 FURTHER RE-SEQUENCING: Epic 6 also goes BEFORE Epic 5's
  # remaining hardening (5-1, 5-2, 5-3). Rationale: Epic 6 is the
  # substantive feature build (the perceived UX delta); 5-1/2/3 are
  # production-hardening that wraps the final feature set. None of
  # 5-1/2/3 block Epic 6 dev work. Mild tradeoff: without 5-1's SIGHUP
  # swap, Epic 6 prompt iterations require manual pipeline restart.
  # Without 5-3's systemd, unattended Epic 6 soaks need manual restart
  # on crash. Both acceptable for dev-window iteration.
  #
  # EXECUTION ORDER (v1 finish line):
  #   Epic 6 (6.1 → 6.2 ∥ 6.3 → 6.4) → 5-1 → 5-2 → 5-3 → 5-4
  #
  # 5-5 is at 'review' (already implemented); it stays where it is —
  # Story 6.2 supersedes 5-5's filler DESIGN but REUSES its
  # cached-audio infrastructure. 5-4's 7-day soak runs last and
  # validates the combined post-Epic-6 + post-hardening system.
  5-5-cached-audio-deterministic-phrases: review
  # 5-4 runs LAST, after Epic 6 lands — see Epic 5 execution-order note above.
  5-4-soak-tuning-and-v1-signoff: backlog
  epic-5-retrospective: optional

  # ───────────────── Epic 6: Conversational Openers (v2 — promotes DR-001) ─────────────────
  # 2026-05-28 design pass (commit ccfc0a0). Initial draft proposed Epic 6
  # (Conversational Openers, 5 stories) + Epic 7 (Speech Timing & Head Motion,
  # 4 stories); consolidated 2026-05-28 on user pushback against
  # over-decomposition into a single cohesive Epic 6 with 4 stories.
  # v2 work. NOT in current sprint.
  #
  # Story order is strict: 6.1 (Cartesia WS migration) is the shared enabler.
  # Story 6.1's TTFB measurement also resolves DR-001's keystone open question.
  # 6.2 and 6.3 can run in parallel after 6.1. 6.4 wraps with instrumentation
  # + soak + olaf-embodiment-brief amendment review (NFR26 spec-as-contract).
  #
  # Coordinates with olaf-embodiment sibling project (consumer-side head/eye
  # realizer must cover the 7th vocalization tag `emphasis` per DR-004).
  # Story 6.2 supersedes Story 5.5's filler design (cached-audio
  # infrastructure reused).
  #
  # See: decision-records.md §DR-001 + §DR-002 + §DR-004 + epics.md §Epic 6.
  epic-6: in-progress
  6-1-cartesia-websocket-and-ttfb-spike: review
  6-2-cached-opener-system-and-cartesia-overlap: review
  6-3-emphasis-vocalization-wiring: review
  6-4-instrumentation-soak-embodiment-brief: review
  # 2026-06-04: Story 6-5 added (Amelia / bmad-create-story). Cartesia->Gemini
  #             TTS provider migration: Live API streaming hot path + batch
  #             cached re-render, behind a [tts] provider knob (Cartesia stays
  #             selectable). Drivers = cost (~35% cheaper/min) + voice range.
  #             Spike-GATED: Task 2 measures Gemini Live TTFB vs the Story 6.1
  #             Cartesia baseline (gate p95 <= 400ms NFR4) before cutover.
  #             24k->16k resample in-client; word timestamps NOT required
  #             (emphasis fires at approximate timing). schema_version stays 3.
  6-5-gemini-tts-migration: in-progress
  epic-6-retrospective: optional

  # ───────────────── v1.5 Backlog (Post-v1) ─────────────────
  # Deferred from v1 in the 2026-05-06 direction shift. Tracked here for v1.5 sprint
  # planning; full acceptance criteria written when v1.5 planning begins. NOT in v1
  # scope for this sprint plan.
  v1.5-1-barge-in: v1.5-backlog
  v1.5-2-cross-restart-mood-persistence: v1.5-backlog
  v1.5-3-expanded-working-submodes: v1.5-backlog
  v1.5-4-configurable-idle-auto-sleep: v1.5-backlog

  # ───────────────── v2 Backlog (Beyond v1.5; smaller than Epic 6) ─────────────────
  # Added 2026-05-28 (commit ccfc0a0 — DR-003 promotion path). Activated only
  # if pipeline-to-dashboard log-format coupling actually starts to bite. The
  # main v2 expression work is captured in Epic 6 above. See:
  # decision-records.md §DR-003 + epics.md §v2 Backlog §Story v2-1.
  v2-1-events-jsonl-structlog-sink: v2-backlog
